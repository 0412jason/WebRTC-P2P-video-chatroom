# WebRTC-P2P-video-chatroom

First, add the ssl certificate in `src/`.  

To run the example:  

`yarn && yarn dev`  
 
or if you use npm:  
 
`npm i && npm run dev`  
 
Run the Web server.  

Select ID of the user and click call.  
